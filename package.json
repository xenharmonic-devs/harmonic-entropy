{
  "name": "harmonix-entropy",
  "version": "0.0.0",
  "description": "Compute harmonic entropy of a musical interval",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "MIT",
  "keywords": ["xenharmonic", "music", "harmonic", "entropy"],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "gts": "^5.2.0",
    "typescript": "~5.2.0",
    "@types/node": "20.8.2"
  }
}
